# Core ML / CV pipeline
mediapipe==0.10.13        # face detection + 478-pt landmarks; ships bundled model weights (no download needed)
opencv-python-headless==4.13.0.92
numpy>=2.0,<3.0
scikit-image==0.26.0      # LBP + HOG descriptors (classical embedder fallback)
scikit-learn==1.8.0       # PCA + LDA discriminant projector, KMeans template clustering
faiss-cpu==1.14.3         # local vector index (stand-in for Milvus/Qdrant -- see app/ml/vector_index.py)
onnxruntime==1.24.4       # only exercised if you drop in ArcFace (buffalo_l) weights -- see app/ml/embedder.py

# API
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32  # multipart/form-data parsing for frame uploads
pydantic==2.13.4

# DB
SQLAlchemy==2.0.51
# psycopg2-binary           # uncomment when DATABASE_URL points at Postgres instead of SQLite

# Auth
PyJWT==2.7.0
